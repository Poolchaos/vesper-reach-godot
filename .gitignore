# The old Vesper Reach web projects (legacy/) - inherited world/art reference only, not part of this repo
legacy/

# Godot 4 engine cache and per-machine imports
game/.godot/
game/android/

# Regenerable slice intermediates (re-create with tools/slice_sheet.py)
game/assets/**/raw/slices/

# Exported builds
/builds/

# OS / editor junk
.DS_Store
*~
*.swp
